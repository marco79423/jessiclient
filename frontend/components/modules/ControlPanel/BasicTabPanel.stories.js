import React from 'react'

import BasicTabPanel from './BasicTabPanel'


export default {
  title: 'modules/ControlPanel/BasicTabPanel',
  component: BasicTabPanel,
}


const Template = (args) => <BasicTabPanel {...args} />

export const Default = Template.bind({})
Default.args = {
}
